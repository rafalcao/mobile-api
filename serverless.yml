service: sls-mobile-api

provider:
  name: aws
  runtime: nodejs16.x
  stage: ${opt:stage, 'dev'}
  region: ${opt:region, 'us-east-1'}
  memorySize: 128
  timeout: 3

functions:
  index:
    handler: index.handler
    description: Lambda Mobile API
    runtime: nodejs16.x
    memorySize: 128
    timeout: 3
    events: